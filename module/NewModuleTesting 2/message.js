let message = () => {
    let name = "Sutha";
    let age = 15;
    return name + " is " + age + " years old."
};

export default message; // can have only 1 default export and it must be in last line of coding